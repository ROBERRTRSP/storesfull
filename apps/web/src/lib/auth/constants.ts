/** Un solo token en cliente (login unificado). */
export const ACCESS_TOKEN_STORAGE_KEY = "ruta_access_token";

/** Cookie httpOnly para el middleware y rutas API (mismo JWT que Bearer). */
export const ACCESS_COOKIE_NAME = "ruta_access";
